import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'newrelic-example-capacitor-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
